import React from 'react';
import MapRouteBuilder from '../components/map-route-builder';
import Header from '@/components/Header';

const MapPage: React.FC = () => {
  return (
    <>

          <MapRouteBuilder />

    </>
  );
};

export default MapPage;
